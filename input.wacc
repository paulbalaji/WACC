# evaluating a moderately complex integer expression

# Output:
# Correct

# Program:

begin
	struct andrea {
		int hi
	}
	struct sam {
		struct andrea a,
		struct sam
	}

	struct sam samInstance = new sam;
	struct andrea aInstance = samInstance.b;
	int i = aInstance.hi

end
